下面按“先稳住 SASRec，再逐步打开 QSD 模块”的顺序跑。建议每个实验单独输出到不同目录，后面 `test_metrics.json` 会自动记录参数。

**1. Pure SASRec Baseline**
```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_sasrec `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 1 `
  --num-hard-neg 0 `
  --num-random-neg 100 `
  --sem-weight 0 `
  --dis-weight 0 `
  --div-weight 0
```

**2. Semantic Fusion Weight**
```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_sem005 `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 4 `
  --num-hard-neg 0 `
  --num-random-neg 100 `
  --sem-weight 0.05 `
  --dis-weight 0 `
  --div-weight 0
```

把 `--sem-weight` 和目录依次改成：

```text
exp_sem010: --sem-weight 0.10
exp_sem020: --sem-weight 0.20
exp_sem050: --sem-weight 0.50
exp_sem100: --sem-weight 1.00
```

**3. Multi-Interest Query**
固定一个较好的 `sem_weight`，比如先用 `0.10`：

```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_interest1_sem010 `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 1 `
  --num-hard-neg 0 `
  --num-random-neg 100 `
  --sem-weight 0.10 `
  --dis-weight 0 `
  --div-weight 0
```

把 `--num-interests` 和目录依次改成：

```text
exp_interest2_sem010: --num-interests 2
exp_interest4_sem010: --num-interests 4
exp_interest8_sem010: --num-interests 8
```

**4. Disambiguation Loss**
固定 `sem_weight=0.10`、`num_interests=4`：

```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_dis002 `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 4 `
  --num-hard-neg 0 `
  --num-random-neg 100 `
  --sem-weight 0.10 `
  --dis-weight 0.02 `
  --div-weight 0
```

把 `--dis-weight` 和目录依次改成：

```text
exp_dis005: --dis-weight 0.05
exp_dis010: --dis-weight 0.10
exp_dis020: --dis-weight 0.20
```

**5. Diversity Loss**
只在 `dis-weight` 已经找到可用值后跑，比如 `0.05`：

```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_div001 `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 4 `
  --num-hard-neg 0 `
  --num-random-neg 100 `
  --sem-weight 0.10 `
  --dis-weight 0.05 `
  --div-weight 0.001
```

再跑：

```text
exp_div005: --div-weight 0.005
exp_div010: --div-weight 0.010
```

**6. Prefix Hard Negative**
最后再打开 hard negative：

```powershell
python scripts\train_qsdrec.py `
  --dataset-dir runs\office `
  --semantic-ids runs\office\semantic_ids_rq.json `
  --output-dir runs\office\exp_hard5_p2 `
  --device cuda `
  --epochs 100 `
  --early-stop-patience 10 `
  --batch-size 256 `
  --max-len 50 `
  --dim 128 `
  --num-interests 4 `
  --prefix-level 2 `
  --num-hard-neg 5 `
  --num-random-neg 100 `
  --sem-weight 0.10 `
  --dis-weight 0.05 `
  --div-weight 0.005
```

改这两组：

```text
exp_hard10_p2: --num-hard-neg 10 --prefix-level 2
exp_hard20_p2: --num-hard-neg 20 --prefix-level 2
exp_hard10_p1: --num-hard-neg 10 --prefix-level 1
exp_hard10_p3: --num-hard-neg 10 --prefix-level 3
```

最小必跑集合建议是：`exp_sasrec`、`exp_sem005/010/020/050`、`exp_interest1/4`、`exp_dis005`、`exp_hard5_p2/hard10_p2`。先别一次性全跑满，先看 `sem_weight` 有没有把全局指标拉起来。
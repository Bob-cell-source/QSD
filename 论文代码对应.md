当前最终主方法是 **LC-SoftSID Full**。代码流程可以分为“离线 Semantic ID 构建”和“在线推荐模型训练”两部分。

**一、核心代码文件**

| 文件 | 作用 |
|---|---|
| [qsdrec/preprocess.py](/毕设新版510/qsdrec/preprocess.py:185) | Amazon 数据预处理、K-core、序列构建 |
| [qsdrec/semantic_id.py](/毕设新版510/qsdrec/semantic_id.py:277) | 文本编码与 RQ-KMeans Semantic ID 构建 |
| [qsdrec/train.py](/毕设新版510/qsdrec/train.py:155) | Soft SID、可靠性、数据划分、训练与评测 |
| [qsdrec/model.py](/毕设新版510/qsdrec/model.py:159) | LC-SoftSID 物品表示与序列推荐模型 |
| [scripts/train_qsdrec.py](/毕设新版510/scripts/train_qsdrec.py:1) | 训练入口 |
| [scripts/run_lc_soft_required_ablations.sh](/毕设新版510/scripts/run_lc_soft_required_ablations.sh:126) | 最终方法配置与模块消融 |
| [scripts/evaluate_lcsoft_group_benchmarks.py](/毕设新版510/scripts/evaluate_lcsoft_group_benchmarks.py:205) | 长尾、孤立 SID、热门 token 等分组实验 |

---

## 二、端到端流程

### 1. 数据预处理

原始交互按照时间排序，执行用户和物品 K-core 过滤，随后映射为连续 ID：

```text
raw reviews + metadata
    → K-core filtering
    → chronological user sequences
    → sequences.json + item_meta.json + stats.json
```

每个用户采用 leave-one-out 划分：

- 训练：除最后两个交互外的前缀样本；
- 验证：倒数第二个物品；
- 测试：最后一个物品。

对应代码在 [train.py](/毕设新版510/qsdrec/train.py:15)。

### 2. 构建 Hard Semantic ID

物品文本由标题、品牌、类目和描述组成：

\[
x_i=\operatorname{Concat}(\text{title},\text{brand},\text{category},\text{description}).
\]

使用预训练文本编码器得到语义向量，再通过逐层残差 K-Means 量化：

\[
z_i=[z_{i,1},z_{i,2},\ldots,z_{i,L}].
\]

每一级聚类上一层剩余的量化残差：

\[
r_i^{(l+1)}=r_i^{(l)}-\mathbf c_{z_{i,l}}^{(l)}.
\]

实现位于 [semantic_id.py](/毕设新版510/qsdrec/semantic_id.py:240)。当前通常使用四级码本。

### 3. 构造局部 SID 邻域

对物品 \(i\) 和 \(j\)，计算相同位置上的 SID token 重叠：

\[
o(i,j)=\sum_{l=1}^{L}\mathbb I[z_{i,l}=z_{j,l}].
\]

保留：

\[
\mathcal N(i)=\{j\ne i\mid o(i,j)\ge \rho\},
\]

其中最终配置为：

\[
\rho=2,\qquad |\mathcal N(i)|\le 50.
\]

这不是只比较 prefix，而是允许任意相同位置上的两个槽位形成邻域。

### 4. Local-Consistent Soft SID

在每个槽位 \(l\) 上统计邻居 token：

\[
n_{i,l}(c)=\sum_{j\in\mathcal N(i)}
\mathbb I[z_{j,l}=c],
\]

局部支持度为：

\[
q_{i,l}(c)=\frac{n_{i,l}(c)}{|\mathcal N(i)|}.
\]

仅保留满足：

\[
q_{i,l}(c)\ge\delta
\]

的候选，其中 \(\delta=0.05\)。

原始 hard token 始终保留，并加入先验计数：

\[
\widetilde n_{i,l}(z_{i,l})
=
n_{i,l}(z_{i,l})+
\lambda_h|\mathcal N(i)|.
\]

最终配置为 \(\lambda_h=1\)。候选分数与权重为：

\[
s_{i,l}(c)=\widetilde n_{i,l}(c)^\eta,
\]

\[
p_{i,l}(c)=
\frac{s_{i,l}(c)}
{\sum_{c'\in Z_{i,l}}s_{i,l}(c')}.
\]

每个槽位保留 Top-\(M\) 个候选：

\[
M=4,\qquad \eta=2.
\]

实现位于 [train.py](/毕设新版510/qsdrec/train.py:155)。

### 5. Semantic Reliability

根据候选 token 的权重及其真实局部支持，计算：

\[
R_i=
\frac{1}{L}
\sum_{l=1}^{L}
\sum_{c\in Z_{i,l}}
p_{i,l}(c)q_{i,l}(c).
\]

代码设置可靠性下限：

\[
R_i\leftarrow\max(R_i,0.1).
\]

注意：当前最终版本没有启用 reliability 与 hard prior 解耦。

### 6. 三部分物品表示

Soft Semantic Basis：

\[
\mathbf b_i=
W_b\left[
\frac{1}{L}\sum_{l=1}^{L}
\sum_{c\in Z_{i,l}}p_{i,l}(c)E_b(c)
\right].
\]

Shared Semantic Residual：

\[
\mathbf r_i^s=
\frac{1}{L}\sum_{l=1}^{L}
\sum_{c\in Z_{i,l}}p_{i,l}(c)E_s(c).
\]

Private ID Residual：

\[
\mathbf r_i^p=E_p(i).
\]

其中 \(E_b\) 和 \(E_s\) 是共享 token embedding，\(E_p\) 是独立 item embedding。

### 7. 可靠性感知残差分配

物品训练频次只由训练部分 `items[:-2]` 统计。私有残差权重为：

\[
\alpha_i=
\frac{f_i}{f_i+\tau R_i},
\qquad \tau=20.
\]

融合残差：

\[
\mathbf r_i=
\alpha_i\mathbf r_i^p+
(1-\alpha_i)\mathbf r_i^s.
\]

最终物品表示：

\[
\mathbf e_i=
\operatorname{LayerNorm}
\left(
\mathbf b_i+\lambda_r\mathbf r_i
\right),
\qquad \lambda_r=1.
\]

具体实现位于 [model.py](/毕设新版510/qsdrec/model.py:276)。

### 8. 用户序列编码

历史物品和候选物品都使用同一个 LC-SoftSID 表示函数。历史序列输入带因果掩码的 SASRec Transformer：

\[
\mathbf h_u=
\operatorname{Transformer}
(\mathbf e_{i_1},\ldots,\mathbf e_{i_t}).
\]

候选物品得分：

\[
\hat y_{u,j}=\mathbf h_u^\top\mathbf e_j.
\]

实现位于 [model.py](/毕设新版510/qsdrec/model.py:297)。

### 9. 实际训练损失

当前主实验使用一个正样本和 100 个随机负样本：

\[
\mathcal C_u=\{i^+\}\cup\mathcal N_u^-.
\]

损失为：

\[
\mathcal L=
-\log
\frac{\exp(\hat y_{u,i^+})}
{\sum_{j\in\mathcal C_u}\exp(\hat y_{u,j})}.
\]

虽然脚本中传入了 `sem_weight`、`dis_weight` 和 `div_weight`，但它们在 `crsid_soft` 中不起作用。由于：

```text
cr_residual_reg = 0
```

所以最终方法实际只使用推荐交叉熵损失，没有辅助语义损失。

---

## 三、最终主方法配置

```text
model_variant                 = crsid_soft
cr_soft_top_m                 = 4
cr_soft_min_overlap_slots     = 2
cr_soft_min_support           = 0.05
cr_soft_support_eta           = 2.0
cr_soft_hard_token_prior      = 1.0
cr_soft_reliability_floor     = 0.1
cr_soft_max_neighbors         = 50
cr_tail_tau                   = 20
cr_residual_scale             = 1.0
cr_alpha_frequency_transform  = raw
cr_soft_lift_kappa            = 0
cr_soft_behavior_weight       = 0
cr_soft_decouple_reliability  = False
```

## 四、论文写作中需要修正的地方

当前文档 [METHOD_LC_SOFT_CRSID_PAPER_20260604_073200.md](/毕设新版510/METHOD_LC_SOFT_CRSID_PAPER_20260604_073200.md:1) 有三处需要注意：

1. **行为邻域不属于最终主方法。** 虽然代码实现了训练集行为共现邻居，但 Office、Beauty、Toys 上都没有改善，最终配置为 `behavior_weight=0`。
2. **不能声称完全解决严重 SID mismatch。** 当前邻域仍要求至少两个 SID 槽位重叠，因此能修复部分 under-sharing，但无法修复只有 0–1 个槽位重叠的严格 mismatch。
3. **核心贡献应描述为完整表示框架。** 即 Local-Consistent Soft SID、Shared/Private Residual Representation 和 Reliability-aware Allocation，而不是只突出 soft 相对 hard 的改动。

因此论文方法主线建议写成：

> LC-SoftSID 首先利用局部 SID 一致性把单路径离散语义标识扩展为多候选分布，然后分别学习语义基底、共享语义残差与物品私有残差，并依据物品频次和局部语义可靠性完成残差分配，最终在统一物品表示中平衡长尾语义泛化与物品级精确记忆。